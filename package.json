{
  "name": "gauge-lsp-tests",
  "version": "0.0.1",
  "description": "LSP tests for Gauge",
  "scripts": {
    "lint": "eslint tests/*.js",
    "test": "npm install && npm run lint && gauge run --tags=\"!knownIssue & (actions_on_project_load | actions_on_file_edit)\" --env=js-wd"
  },
  "license": "MIT",
  "dependencies": {
    "ncp": "^2.0.0",
    "tmp": "^0.0.33",
    "vscode-jsonrpc": "^4.0.0",
    "vscode-uri": "^1.0.5"
  },
  "devDependencies": {
    "@actions/artifact": "latest",
    "@actions/core": "latest",
    "@actions/exec": "latest",
    "@getgauge/cli": "^1.1.5",
    "eslint": "^5.14.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/getgauge/gauge-lsp-tests.git"
  }
}
