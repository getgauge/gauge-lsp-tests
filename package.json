{
  "name": "gauge-lsp-tests",
  "version": "0.0.1",
  "description": "API tests for Gauge Language Server",
  "scripts": {
    "eslint" : "./node_modules/.bin/eslint tests",
    "test": "npm run eslint && gauge run specs"
  },
  "dependencies": {
    "vscode-jsonrpc": "^3.4.1"
  },
  "devDependencies": {
    "eslint": "^4.8.0"
  }
}
